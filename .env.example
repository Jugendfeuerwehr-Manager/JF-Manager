# ==============================================
# JF-Manager Production Environment Configuration
# ==============================================

# PostgreSQL Configuration
POSTGRES_DB=jf_manager_backend
POSTGRES_USER=jf_manager
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD

# Django Configuration
DJANGO_SECRET_KEY=CHANGE_ME_TO_RANDOM_50_CHARS
DJANGO_ADMIN_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
DJANGO_ADMIN_EMAIL=admin@yourdomain.com
DJANGO_MANAGEPY_MIGRATE=on
DEBUG=False

# Security Settings
# ALLOWED_HOSTS: Comma-separated list of domains (no protocol)
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,localhost
# CSRF_TRUSTED_ORIGINS: Comma-separated list of FULL URLs including protocol
# Must include https:// or http:// prefix for each domain
CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,http://localhost

# Email Configuration
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=your_email@example.com
EMAIL_HOST_PASSWORD=your_email_password
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# Backup Configuration
BACKUP_KEEP_DAYS=7
BACKUP_KEEP_WEEKS=4
BACKUP_KEEP_MONTHS=6

# Frontend Configuration
# For production behind nginx reverse proxy, use relative path:
VITE_API_BASE_URL=/api/v1
# For development or direct access, use full URL:
# VITE_API_BASE_URL=https://yourdomain.com/api/v1

# Port Configuration (optional, defaults to 80/443)
HTTP_PORT=80
HTTPS_PORT=443

# Build Information (optional)
BUILD_DATE=
VCS_REF=
